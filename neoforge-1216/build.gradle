// neoforge-1216/build.gradle
plugins {
    id 'net.neoforged.moddev' version "${mdg_version}"
}

base {
    archivesName = "PrivateDimension-neoforge-${mc_1216}"
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(java_1216.toInteger())
    }
}

sourceSets {
    main {
        java      { srcDirs 'src/main/java',      '../common/src/main/java' }
        resources { srcDirs 'src/main/resources', '../common/src/main/resources' }
    }
}

neoForge {
    version = neoforge_1216
    runs {
        server { server() }
    }
    mods {
        privatedimension {
            sourceSet sourceSets.main
        }
    }
}

processResources {
    inputs.property 'version',          project.version
    inputs.property 'mc_version',       mc_1216
    inputs.property 'neoforge_version', neoforge_1216
    filesMatching('META-INF/neoforge.mods.toml') {
        expand(version: project.version, mc_version: mc_1216, neoforge_version: neoforge_1216)
    }
}

jar {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    manifest {
        attributes 'Implementation-Title': 'privatedimension',
                   'Implementation-Version': project.version
    }
}
